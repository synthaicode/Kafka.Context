# 進捗ログ運用（Kafka.Context）

## 目的
- 「何を決めたか」「なぜそうしたか」を後から追えるようにする。
- 不明点は「わからない」を歓迎し、先に可視化して詰まらないようにする。

## 記録先
- 設計差分（決定事項/変更点）: `docs/diff_log/`
- 機能の作業メモ（実装・テスト・検証の単位）: `features/{機能名}/`

## ルール（最小）
- 不明点が残る場合は、必ず「未解決の論点」を残して次アクションを明記する。
- API の変更/追加など、外部に影響する決定は必ず diff_log を追加する。

